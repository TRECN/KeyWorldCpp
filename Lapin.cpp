#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        string a;
        int m;
        getline(cin,a);
        if(s.length()%2==0)
            m=s.length()/2;
        else
            m=(s.length()/2)+1;
        vector<int> m1;
        for(int i=0;i<m;i++){
            char c=
        }
    }
    return 0;
}