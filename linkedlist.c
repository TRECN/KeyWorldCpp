#include<stdio.h>
struct node{
    int data;
    struct node* next;
};

struct node* start;

void create(){
    char ch;
    struct node* p,*q;
    p=(struct node*)malloc(sizeof(struct node));
    scanf("%d",&p->data);
    start
}

void main(){

}